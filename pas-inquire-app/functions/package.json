{
  "name": "pas-inquire-functions",
  "description": "CORS-bypass proxy + OAuth broker for the PAS $inquire Tester",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,hosting,auth",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "express": "^4.21.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  }
}
